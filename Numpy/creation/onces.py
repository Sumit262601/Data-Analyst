#ones(shape, dtype) creates an array of ones with the given shape and data type
#ones(shape) creates an array of ones with the given shape

import numpy as np
ones_array = np.ones((3, 2), dtype=float)
print(ones_array)